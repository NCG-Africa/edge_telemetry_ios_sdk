// Internal — invisible from `import EdgeRum`.
//
// Capture surfaces (UIKit screens, HTTP, taps, samplers, …) land with
// F6–F12. This stub keeps the target non-empty during F1 bootstrap.

import Foundation

internal enum EdgeRumCaptureModuleStub {
    internal static let marker: String = "EdgeRumCapture"
}
